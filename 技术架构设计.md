# 求职天赋测评：技术架构设计与选型说明

**版本**：v0.1
**日期**：2026-09-12
**适用范围**：MVP（三页面、40题测评、可解释报告、管理员规则/知识库维护）

## 1. 目标与约束

系统需要完成“首页 → 40题测评 → 人才画像报告”的闭环，并使用用户选择的魔搭 AIGC 内容生成与 API Inference 在线推理能力，结合已确认规则、用户答案和职场知识库生成可分享报告。规则和知识库本期由管理员进行简单编辑、发布和回滚。

赛事约束包括：部署到魔搭创空间且路演时保持 Running；使用真实脱敏数据进行对照演示；输出应可解释、可核验、可修正；密钥必须放在创空间 Secrets 中。产品约束包括：桌面 Web 优先并兼容移动端；未知信息显示“待验证”而不是伪造分数；报告版本可追溯；AI 输出必须经过本人再判断。

## 2. 总体架构

```text
React 用户端 / 管理端
          │ REST/JSON
          ▼
FastAPI 应用层
 ┌──────────────────────────────────────────────┐
 │ 测评与草稿 │ 规则判断 │ 知识库检索 │ 报告生成 │
 │ 岗位匹配   │ 反馈追踪 │ 管理后台   │ 审计日志 │
 └──────────────────────────────────────────────┘
       │                 │                 │
       ▼                 ▼                 ▼
   SQLite/PostgreSQL   Chroma      魔搭 AI 服务
   结构化数据          向量索引           AIGC/API Inference
```

系统采用模块化单体。前后端通过 REST API 通信；确定性证据判断和报告生成分离。规则负责来源状态、去重、冲突、任务覆盖、资格、缺口与未知；AI只负责候选提取、语义对应建议和锁定结构化判断后的解释。当前缺完整数值锚点、人才类型分类器和冲稳保阈值，因此MVP不强制数值雷达、适配百分比或策略档位。

## 3. 技术选型与判断原因

| 技术 | 选择 | 判断原因 | 本期边界 |
|---|---|---|---|
| 前端 | React + TypeScript + Vite | 组件化适合三页面、抽屉、分步答题和管理后台；TypeScript 可约束报告 JSON；Vite 启动和构建快 | 不引入 SSR 和复杂全栈框架 |
| UI | Tailwind CSS + Design Tokens | 快速复用黑紫视觉、响应式断点和状态样式；便于统一按钮、卡片、图表和可访问性 | 只维护一套主题，不做复杂主题系统 |
| 状态 | Zustand | 适合保存答题草稿、报告加载状态和管理员编辑状态，代码量小 | 服务端数据仍以 API 为准 |
| 表单 | React Hook Form + Zod | 支持40题校验、多选/经历题和提交前完整性检查；前后端可共享字段约束思路 | 不把前端校验当作最终校验 |
| 图表 | Recharts | 展示有定义的数据与任务覆盖 | 未定义数值不绘制雷达或适配百分比 |
| 后端 | FastAPI + Pydantic | Python 生态适合规则、检索和 AI 编排；自动生成 OpenAPI；Pydantic 适合校验结构化模型输出 | 采用模块化单体，不拆微服务 |
| 数据库 | SQLite（MVP），PostgreSQL（后续） | SQLite 零配置、适合赛事快速部署；数据模型和 SQLAlchemy 可平滑迁移 PostgreSQL | 不依赖容器临时目录保存重要数据 |
| ORM/迁移 | SQLAlchemy + Alembic | 支持版本迁移、关联查询和 PostgreSQL 迁移 | 迁移脚本纳入仓库 |
| 检索 | Chroma（嵌入式持久化模式） | 内置文档、元数据过滤及增删能力，更适合管理员频繁更新；FAISS 还需自行维护文档映射与删除逻辑 | 不引入独立向量数据库服务 |
| AI | 魔搭 AIGC / API Inference | 用户选择的在线推理平台，可按已验证可用模型切换 | 赛事手册未限定模型供应商；模型名、URL、Key 均由环境变量配置 |
| 部署 | Docker → 魔搭创空间 | 后端、前端构建和依赖可固定；适合路演前重复部署和日志排查 | 服务监听 `0.0.0.0:7860` |

## 4. 核心业务链路

### 4.1 测评与报告

```text
创建会话 → 自动保存答案 → 提交并锁定版本
→ 校验题目/答案与复合输入 → 规范化证据、去重和冲突分组
→ 资格/覆盖/缺口判断 → 可用时检索知识库 → 调用魔搭模型
→ JSON Schema 校验 → 保存待复核 ReportVersion
→ 本人复核 → 展示和生成分享卡
```

规则优先回答证据、自述、迁移、缺口、未知和冲突。岗位资格使用`current|explore|clarify|exclude`；投递策略另存`strategyLabel`与`strategyStatus`。当前推荐0—3个，明确目标另列最多3个。知识库提供有来源的方法解释；模型只组织锁定判断。

### 4.2 规则和知识库发布

```text
管理员编辑草稿 → 一致性校验 → 构建规则快照/知识库索引
→ 验证成功 → 原子切换生效指针 → 新任务读取新版本
```

历史报告固定引用生成时的规则版本、题目版本、知识库索引版本、模型和 Prompt 版本，不因后台更新而改变。

## 5. AI 与知识库设计

统一封装 `LLMProvider`：

```python
class LLMProvider:
    async def generate_report(self, payload) -> dict: ...
    async def embed(self, texts: list[str]) -> list[list[float]]: ...
```

报告 Prompt 必须包含：用户答案、锁定的规则判断、证据、目标岗位/行业约束、检索片段、输出 JSON Schema 和“事实/推断/建议/待验证”表达要求。仅在已发布数值方法存在时包含数值结果。模型输出先经过 Pydantic 校验，再检查证据引用、未知值和敏感字段。

知识库文档按标题和段落切分，保存 `documentId`、`chunkId`、来源、版本和更新时间。检索结果带 `knowledgeEvidenceIds`，报告可追溯到具体知识片段。规则文件与知识库不能直接拼成无边界长 Prompt。

## 6. 数据模型

核心对象：`User`、`AssessmentSession`、`AssessmentAnswer`、`QuestionVersion`、`RuleVersion`、`KnowledgeDocument`、`KnowledgeChunk`、`KnowledgeIndexVersion`、`ReportVersion`、`EvidenceItem`、`JobMatch`、`ActionItem`、`FeedbackEvent`、`AdminUser`、`AuditLog`。

`ReportVersion` 至少保存：

```text
reportId, assessmentId, questionVersion, ruleVersion,
knowledgeIndexVersion, modelName, promptVersion,
inputHash, reviewStatus, createdAt
```

未知使用`null`；每个结论绑定`evidenceIds`；任务覆盖按唯一核心任务去重，分母为0返回`null`；长解释保存为`sections`。

## 7. 管理后台 MVP

1. **专家规则**：编辑证据、资格、覆盖、缺口规则和未来经确认的数值锚点；草稿、发布、回滚。
2. **题目版本**：维护唯一40题题库；引用中的版本不可覆盖，只能新建版本。
3. **知识库**：上传 Markdown/TXT，编辑标题和分类，重建索引，启用/停用文档。
4. **报告配置**：选择已发布规则、知识库索引、模型和 Prompt 模板；查看生成失败原因。

本期不做复杂权限分级，至少保留管理员身份校验、发布操作人、时间、变更原因和审计日志；用户会话、越权访问和 CSRF 仍必须实现。

## 8. 安全、可靠性与降级

- API Key、Token、数据库密码只放魔搭 Secrets/环境变量，不进代码、README、截图或录屏。
- 单次AI生成或校验失败只重试一次，仍失败使用规则模板；队列租约故障另有最多3次任务尝试。
- 模型或知识库不可用时明确降级；有有效证据即可生成最小报告，不因缺数值规则而阻断。
- 用户提交使用幂等键，防止重复生成报告。
- 报告生成任务保存状态：`queued|running|ready|failed`。
- 重要数据需挂载持久化存储或外部数据库；不能依赖容器重启后仍存在的临时文件。

## 9. 部署与验收

Docker 容器统一监听 `0.0.0.0:7860`，部署到魔搭创空间后检查 Build/Run 日志、公开访问、Secrets 和 Running 状态。提交前用未登录窗口跑通完整链路，并用两份脱敏用户数据对照证明输出存在实质差异。

验收重点：40题版本唯一；每个结论可追溯到答案/规则/知识库；管理员更新后新报告读取新版本；旧报告不变；未知值不补造；本人复核状态可见；移动端无横向溢出；分享卡不包含题目作答和风险详情。

## 10. 后续演进

当真实反馈量增加后，将 SQLite 迁移 PostgreSQL，将本地向量索引迁移到托管向量库；再增加 JD 解析、开放式陪伴 Chat、完整个人档案、T+7/T+30/T+90 正式回填和复测对比。演进不改变“规则确定性计算、知识库提供依据、AI 组织表达、本人最终判断”的边界。

## 11. 实施细化与关键约定

### 11.1 发布的一致性

“实时更新”指管理员无需重新部署即可编辑，发布成功后的新生成任务立即使用新版，不是每次敲字直接影响线上结果。知识库先在新集合完成切分、索引和检索抽查，再原子切换已发布版本指针；失败继续使用旧版。回滚同样切换指针，保留所有被历史报告引用的版本。

开始答题时固定题目版本；提交时固定兼容规则、知识库、Prompt版本。发布校验唯一Q01—Q40、Q39可选、复合输入结构与规则兼容性；数值锚点未齐时禁用数值输出，不阻断证据最小报告。

管理员用表单编辑数值规则，用 Markdown 文本框编辑专家解释原则。规则仅允许白名单运算和配置，不执行管理员上传的 Python、JavaScript 或任意表达式。

### 11.2 模型接入与知识检索

魔搭 AIGC 与 API Inference 是能力入口，不假设二者具有相同接口，也不假设所有模型都支持 OpenAI 兼容协议、Embedding 或 JSON Schema。后端适配器分别封装文本生成与向量生成；接入前实测端点、模型 ID、权限、额度、上下文长度和结构化输出能力。目前没有完成账户级验证，不承诺具体模型可用。

首选使用魔搭可用的中文指令模型生成报告。Embedding 优先采用已验证可调用的魔搭服务；无对应接口时使用 CPU 可运行的多语种 Sentence Transformers 模型作为明确降级，并固定模型版本。变更 Embedding 模型必须重建整版索引，不混用不同模型的向量。

首版采用“六类方法论标签过滤 + 向量 Top-K 检索”，默认召回 6 个片段，按上下文预算截断。六类标签对应：需求与筹码、分步补牌、点线面体、减少过早自筛、本人再判断、痛点到交付。以固定样例验证召回后再调整参数。行业判断必须保留原文日期，过期资料不作为当下趋势事实。

知识库包含周报、营销内容、案例、图片和外链。首期只索引已审阅的相关文字，图片和未读取的外链标为未解析，不假装已获取其内容。保留原文出处和文档内部位置；检索文本和用户输入作为资料，不得覆盖系统规则或授权档案写入。

### 11.3 评分边界与内容缺口

《题目与AI报告生成规则》已提供唯一完整Q01—Q40，但仍缺完整逐选项锚点、人才类型分类器、岗位阈值和冲稳保方法。不得补造。Q30的1—4级自述不直接映射能力L0—L3；Q07/Q40答案不同不自动判矛盾、虚假或扣分。当前输出无精确分数的证据画像。

开放经历题可由 AI 抽取候选行为与成果，但必须引用原答案片段，区分“用户自述”与“经验证成果”；不能因用户写了一个作品链接就判定作品已核验。无有效评分规则时不计入确定性分数。

资格`current|explore|clarify|exclude`与投递策略分离。方法未核实时`strategyLabel=null`、`strategyStatus=pending_method`；证据不足为未知或迁移待验证，冲突进入clarify。

### 11.4 任务与分享

生成请求先持久化任务并返回jobId。队列记录租约并最多尝试3次；每次任务内部AI生成失败只重试一次，仍失败使用规则模板。两类重试分别计数，固定输入快照保证结构化判断可复现。

报告生成状态与本人复核状态分开：模型草稿先保存为待复核，用户确认不代表客观真实性已被证明。分享卡读取独立字段白名单：一句话定位和产品标识始终可用；类型和雷达仅在对应确认规则已发布且结果非null时包含；通过前端模板与 html-to-image 导出 PNG，预览后确认生成。AI 生成报告文字，文字、数值和图表采用确定性排版，不依赖文生图绘制。若后续调用魔搭生图生成装饰素材，使用独立接口，失败不阻断报告。

### 11.5 数据持久化与访问控制

SQLite 与 Chroma 均需实测重启和重新部署后的持久化。若创空间无可靠持久卷，线上结构化数据改用外部 PostgreSQL；知识原文/版本保存在数据库或私有对象存储，Chroma 索引可重建。共享方案不得将不同实例的 SQLite 文件当作共同数据库；本期固定单实例。

用户会话采用服务器签发的不可猜测凭证与 HttpOnly Cookie，后端校验报告归属；管理员密码采用安全哈希并建立服务端会话，限制登录尝试，写操作校验 CSRF。草稿本地缓存配合服务端保存，UI 区分“已存本机”和“已同步”；退出或清除数据时处理本机缓存。公开页面不能直接访问管理员接口或其他用户完整报告。

## 12. 验证方法与来源

验证采用 pytest（评分、版本发布、幂等、权限）、Vitest（前端关键数据映射）和 Playwright（答题恢复、完整报告、管理员发布、分享预览）。模型专项验证使用固定脱敏样例，检查引用有效性、事实与推断区分、未知值处理和个人差异，不以语言流畅度代替准确性。发布更新、任务进行中更新、回滚和容器重启均需覆盖。

本文依据以下已提供资料与用户确认的补充要求编写；资料中的操作指令作为需求背景，不视为授权安装、部署、公开上传或联系他人：

- `中客松2026选手手册 20260911.pdf`（外部材料）：部署、评分及数据留痕约束。
- `产品设计PRD-求职天赋测评三页面.md`（外部材料）：页面、证据、分享、版本与扩展要求。
- `题目与AI报告生成规则.md`（已整理至 `backend/seed/sources/`）：唯一现行题表、证据、岗位和报告规则源；其裁定优先于旧规则文件与早期示例。
- `【0-100W职场跃迁研习社】重磅知识库.md`（外部材料）：方法论和案例来源，需经清洗后入库。

文档范围为技术设计，不代表接口已联通、内容已全部核验或系统已实现。PRD 中 Chat 首期范围存在不同表述；本设计按已确认三页面方案预留接口，首期实现受限报告问答、候选事实确认和可查看的最小档案；开放式陪伴、完整档案界面与自动追踪列入后续迭代。

## 13. 本次冲突裁定与实现基线

本期包含报告内“理解报告”问答和最小档案闭环：AI 从当前报告与已确认事实回答；新信息生成候选事实卡，用户确认后进入新的 ProfileSnapshot，并显示来源和时间。未确认内容仅作会话上下文。复测与报告生成读取有效已确认事实，保留来源，不能替代本次必答题。个人档案用报告详情加已确认事实列表实现，不新增复杂独立页面。开放式聊天、自动提醒和完整长期跟踪后移。

采用固定声明式证据配置，不开发通用规则语言。AI抽取候选证据须保留原文并经确认；模型不能修改资格、覆盖、证据、迁移、策略或版本。

档案更新可基于冻结答案和新版确认事实重算报告，无需重答40题。文件、语音、完整Company长期追踪属于新增范围待办；MVP仍为三页面、最小档案和手工经历入口，不代表这些扩展已实现。

正式报告规则必须经专家确认，但平台联通测试不依赖40题内容齐备。先验证魔搭文本/向量能力与持久化，再实现业务；缺内容时使用标记清楚的合成测试夹具，不冒充真实试用。魔搭推理是用户补充选择，赛事手册本身并未限制模型供应商。
